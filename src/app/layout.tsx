import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/providers/theme-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "US Economic Data | Real-Time Economic Indicators",
  description: "Premium visualization of US economic indicators including GDP, inflation, employment, interest rates, and more. Real-time data from official sources.",
  keywords: ["US economy", "economic data", "GDP", "inflation", "CPI", "employment", "Federal Reserve", "economic indicators"],
  authors: [{ name: "US Economic Data" }],
  openGraph: {
    title: "US Economic Data",
    description: "Premium visualization of US economic indicators",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
